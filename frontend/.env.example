# Frontend API base URL
# Local development can omit this because Vite proxies /api to the backend.
# Production should point to your deployed backend API.
VITE_API_URL=https://hospital-project-uojh.onrender.com/api
